# Dependencies
node_modules/

# Lockfiles for other package managers (we use npm)
yarn.lock
pnpm-lock.yaml
bun.lockb

# Note: Root package-lock.json IS committed (important for reproducible builds)
# But workspace-level lockfiles should NOT exist (npm workspaces use root lockfile only)
apps/**/package-lock.json
packages/**/package-lock.json

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.development
.env.test
.env.production

# Logs
*.log
npm-debug.log*

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Apps and packages are Git submodules
# They are tracked via .gitmodules file